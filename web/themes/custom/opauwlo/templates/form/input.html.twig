{% set inputType = attributes.type|render %}
{%
  set classes = [
    inputType == 'submit' ? 'btn btn-success fw-bold text-light' : 'form-control',
  ]
%}
<input{{ attributes.addClass(classes) }} />{{ children }}
